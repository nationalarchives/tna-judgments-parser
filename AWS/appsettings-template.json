{
    "AWS": {
        "AccessKeyId": "",
        "SecretAccessKey": "",
        "AsyncFunctionName": "{ name of the second asyncronous function, same as 'function-name' in aws-lambda-tools-defaults2.json }",
        "RegionEndpoint": "{ region of the lambda function, e.g., 'eu-west-2' }",
        "Bucket": "{ name of the S3 bucket into which asyncronous responses are stored }"
    }
}